# Server
PORT=5000
NODE_ENV=development

# Database (SQLite — no setup needed, file auto-created)
DATABASE_PATH=./data/smartqueue.db

# JWT
JWT_SECRET=smartqueue-dev-jwt-secret-key-2024
JWT_REFRESH_SECRET=smartqueue-dev-refresh-secret-key-2024
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d

# CORS
CORS_ORIGIN=http://localhost:3000
